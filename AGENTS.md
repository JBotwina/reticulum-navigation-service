# Agent Instructions

## Validation Commands

- Do not run build commands to validate changes.
- For checks, run Biome commands only.
- Preferred validation command: `bun --bun run check` in `destination-app`.
- Optional Biome commands when needed: `bun --bun run lint` and `bun --bun run format`.
